package corejava.annotation;

import java.lang.annotation.*;
import java.lang.reflect.*;
import corejava.annotation.Getter;

public class TestAnnotation {

	public static void main(String[] args) throws Exception {
		
		Class<Employee> obj = Employee.class;
		Field[] fields = obj.getDeclaredFields();
		
		for(int i=0;i<fields.length;i++)
			System.out.println(fields[i].getName().toString().substring(0, 1));
		
		for (Method method : obj.getDeclaredMethods()) {
			
			if (method.isAnnotationPresent(Getter.class)){
				if(method.getName().length()>3){
					if(method.getName().substring(0, 3).equals("get")){
						boolean flag=false;
						for(int i=0;i<fields.length;i++){
							System.out.println("Getter Successfgfg"+method.getName().toString().substring(3, 4).equals(fields[i].getName().toString().substring(0, 1).toUpperCase()));

							if(method.getName().toString().substring(3, 4).equals(fields[i].getName().toString().substring(0, 1).toUpperCase())){
								System.out.println("Getter Success");
							}

						}
						
					}
					else{
						System.out.println("Getter Error");
					}
				}
				else{
					System.out.println("Getter Error");
				}
			}
		}
		
	}
}
